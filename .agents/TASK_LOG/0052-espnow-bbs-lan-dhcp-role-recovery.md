# Task Log 0052 - ESP-NOW BBS LAN DHCP Role Recovery

## Metadata

- ID: 0052-espnow-bbs-lan-dhcp-role-recovery
- Local bench date: 2026-05-25 America/Denver
- UTC evidence date: 2026-05-26
- Status: implemented; graceful cleanup complete and read-only preflight passed
- Roles: Communications, QA, Agent operations

## Objective

Refactor the live preflight for current esptool command spelling and recover the
bench network from Windows ICS mode to existing LAN DHCP while validating the
current `COM6`/`COM10`/`COM12` peer role map.

## Verified Facts

- `scripts/live_bench_preflight.py` now uses hyphenated `chip-id`,
  `read-mac`, and `flash-id` for fresh identity probes.
- Legacy underscore identity command records still parse for old evidence.
- Preflight summaries now include esptool warning lines for peers and the
  coordinator.
- The current peer remap shortcut maps by accepted MACs with
  `peer01=COM6`, `peer02=COM10`, and `peer03=COM12`.
- Pi-side coordinator probes now prefer the existing Pi venv
  `/home/dospi/dos-c/artifacts/runtime/esptool-venv-espnow-encrypted/bin/esptool`
  before falling back to older system esptool commands.
- Windows ICS was disabled on `Wi-Fi` and `Ethernet 3`.
- `Ethernet 3` renewed on LAN DHCP as `192.168.200.225/24` with gateway
  `192.168.200.1`.
- LAN discovery found the Pi at `192.168.200.153` with expected SSH
  fingerprints.
- Final read-only preflight matched the accepted peer MACs and coordinator
  `/dev/ttyUSB0` MAC `78:e3:6d:10:4d:6c`.
- Fresh pre-cleanup SSH verification on 2026-05-26 UTC matched the expected Pi
  RSA, ECDSA, and ED25519 fingerprints.
- The only pre-cleanup stale Pi process was PID `1346` with command
  `dosbox-x -conf /home/dospi/dos-c/devices/pi4-poe/win31.conf -nomenu -fullscreen`.
- No Pi listener was present on `31331`, `31332`, or `8080` before cleanup.
- A normal `SIGTERM` to PID `1346` removed the stale `dosbox-x` process; no
  `SIGKILL` was used.
- Post-cleanup process/listener checks found no related DOSBox-X, DOSBox,
  bridge, modal, or `31331`/`31332`/`8080` listener state.
- Post-cleanup read-only preflight
  `research/bench-records/live-bench/espnow-bbs-lan-dhcp-current-remap-preflight-20260526T032217Z.json`
  returned `ok:true` with summary readiness `ready_for_prepare`.

## Assumptions

- Since `/dev/ttyUSB0` is already the expected coordinator MAC, recovery flashing
  is not required and should stay closed.
- The accepted serial-nullmodem path remains unchanged.
- Cleanup authorization was limited to graceful termination of the exact stale
  process only.

## Unknowns

- Physical USB-only/no-load/no-relay/TFT/MicroSD/XBee wiring state is not proven
  by the read-only preflight.
- No live ESP-NOW radio traffic is generated by this preflight.

## Stop Gates

- Stop before `prepare` or `flash`; the coordinator identity is already correct.
- Stop before bridge/DOSBox-X/Win31 proof unless a new explicit proof run is
  requested.
- Stop on any future MAC mismatch, Pi fingerprint mismatch, backup/hash failure,
  write failure, or verify failure.

## Validation

- `python3 -m unittest tests.live_bench.test_multipeer_preflight tests.live_bench.test_espnow_bbs_live_gate`: passed.
- `python3 -m unittest discover tests/live_bench`: passed.
- `python3 scripts/verify_scaffold.py`: passed.
- `git diff --check`: passed.
- Final live preflight evidence:
  `research/bench-records/live-bench/espnow-bbs-lan-dhcp-current-remap-preflight-20260526T030752Z.json`
  returned `ok:false` with the single failure `stalePiProcess`.
- Cleanup resume checks:
  - Fresh SSH keyscan/known-hosts verification matched the expected Pi
    fingerprints.
  - Pre-cleanup process/listener check found only the exact PID `1346`
    `dosbox-x` blocker and no `31331`/`31332`/`8080` listeners.
  - `SIGTERM` removed PID `1346`; post-cleanup process/listener check was clear.
- Post-cleanup read-only preflight:
  `research/bench-records/live-bench/espnow-bbs-lan-dhcp-current-remap-preflight-20260526T032217Z.json`
  returned `ok:true` with no failures.

## Handoff

- Cleanup-resolved handoff:
  [../handoffs/0041-espnow-bbs-lan-dhcp-role-recovery-blocked.md](../handoffs/0041-espnow-bbs-lan-dhcp-role-recovery-blocked.md).
