# CBBS Client Source

This app uses Expo Router and workspace packages only. It reads local fixtures,
dispatches local UI intents, and does not contain live transport, native build,
EAS, App Center, BLE, Web Serial, Web Bluetooth, SoftAP, or serial-write code.
