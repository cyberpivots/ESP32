# Source Ledger - 2026-05-25 ESP-NOW BBS LAN DHCP Current Remap

Source index: [../source-index.md](../source-index.md)

## Scope

Bench-network recovery for the ESP-NOW BBS live gate: disable Windows ICS,
restore `Ethernet 3` to LAN DHCP, discover the Pi on the LAN, update preflight
tooling for hyphenated esptool identity commands, and validate the current
`COM6`/`COM10`/`COM12` peer remap.

No prepare, flash, erase, monitor, bridge proof, Win31/OPCON run, PCAP,
router/admin mutation, BLE, mesh, relay, XBee, TFT, MicroSD, load, or mains
action was run.

## Evidence

- Preflight implementation:
  `scripts/live_bench_preflight.py`.
- Live-gate helper implementation:
  `scripts/espnow_bbs_live_gate.py`.
- Tests:
  `tests/live_bench/test_multipeer_preflight.py`,
  `tests/live_bench/test_espnow_bbs_live_gate.py`.
- Ignored Windows inventory:
  `research/bench-records/live-bench/windows-lan-dhcp-inventory-20260526T030425Z.json`.
- Ignored Windows DHCP action evidence:
  `research/bench-records/live-bench/windows-lan-dhcp-apply-20260526T030457Z.json`.
- Ignored LAN SSH scan:
  `research/bench-records/live-bench/windows-lan-dhcp-ssh-scan-20260526T030513Z.json`.
- Ignored final preflight:
  `research/bench-records/live-bench/espnow-bbs-lan-dhcp-current-remap-preflight-20260526T030752Z.json`.
- Ignored post-cleanup preflight:
  `research/bench-records/live-bench/espnow-bbs-lan-dhcp-current-remap-preflight-20260526T032217Z.json`.
- Tracked bench record:
  `research/bench-records/live-bench/2026-05-25-espnow-bbs-lan-dhcp-current-remap-preflight.md`.
- Task record:
  `.agents/TASK_LOG/0052-espnow-bbs-lan-dhcp-role-recovery.md`.
- Handoff:
  `.agents/handoffs/0041-espnow-bbs-lan-dhcp-role-recovery-blocked.md`.

## Verified Facts

- Windows started with ICS enabled from `Wi-Fi` to `Ethernet 3`, and
  `Ethernet 3` held `192.168.137.1/24`.
- Windows saw the current peer adapters on `COM6`, `COM10`, and `COM12`.
- Disabling ICS on `Wi-Fi` and `Ethernet 3`, enabling IPv4 DHCP on
  `Ethernet 3`, resetting DNS, and renewing DHCP succeeded.
- `Ethernet 3` then held `192.168.200.225/24` with gateway `192.168.200.1`.
- LAN SSH discovery found `192.168.200.153`, and that host presented the
  expected Pi SSH fingerprints.
- The final preflight matched accepted peer role MACs:
  `peer01=COM6`, `peer02=COM10`, `peer03=COM12`.
- Pi `/dev/ttyUSB0` matched coordinator MAC `78:e3:6d:10:4d:6c`, so no
  recovery flash is needed.
- The preflight summary records esptool warnings from `chip-id` output instead
  of silently dropping them.
- Fresh pre-cleanup SSH keyscan/known-hosts verification on 2026-05-26 UTC
  matched the expected Pi RSA, ECDSA, and ED25519 fingerprints.
- The only pre-cleanup related Pi process was PID `1346` with command
  `dosbox-x -conf /home/dospi/dos-c/devices/pi4-poe/win31.conf -nomenu -fullscreen`.
- No Pi listener was present on `31331`, `31332`, or `8080` before cleanup.
- A normal `SIGTERM` removed PID `1346`; no `SIGKILL` was used.
- Post-cleanup checks found no related DOSBox-X, DOSBox, bridge, modal, or
  `31331`/`31332`/`8080` listener state.
- Post-cleanup read-only preflight passed with `ok:true`, no failures, and
  summary readiness `ready_for_prepare`.

## Assumptions

- Cleanup authorization was limited to graceful termination of the exact stale
  process only.
- The accepted serial-nullmodem path remains unchanged.

## Unknowns

- Physical USB-only/no-load/no-relay/TFT/MicroSD/XBee wiring state is not proven
  by the read-only preflight.
- No live ESP-NOW radio traffic is generated by this preflight.

## Stop Gate

The stale Pi `dosbox-x` process blocker is resolved by graceful cleanup and a
fresh `ok:true` read-only preflight. Do not run `prepare` or `flash` because
the coordinator role is already present on `/dev/ttyUSB0`. Do not start a
bridge/DOSBox-X/Win31 proof run unless a new explicit proof run is requested.
