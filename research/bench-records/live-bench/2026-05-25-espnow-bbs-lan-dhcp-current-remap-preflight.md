# ESP-NOW BBS LAN DHCP Current Remap Preflight - 2026-05-25

## Record Metadata

- Record ID: 2026-05-25-espnow-bbs-lan-dhcp-current-remap-preflight
- Local bench date: 2026-05-25 America/Denver
- UTC evidence date: 2026-05-26
- Operator: Codex
- Workspace: `/mnt/h/ESP32`
- Scope: Windows LAN DHCP recovery, current three-peer COM remap, Pi LAN
  discovery, and read-only coordinator identity.
- Excluded work: prepare, flash, erase, monitor, bridge proof, DOSBox-X launch,
  Win31/OPCON run, serial-write expansion, PCAP, router/admin mutation, BLE,
  mesh, relay, XBee, TFT, MicroSD, load, and mains.

## Source Coverage

- Source IDs reviewed:
  `SRC-LOCAL-ESPNOW-LAN-DHCP-CURRENT-REMAP-2026-05-25`,
  `SRC-ESPTOOL-BASIC`,
  `SRC-LOCAL-ESPNOW-LIVE-GATE-TOOLING-2026-05-23`.
- Ignored evidence:
  - `research/bench-records/live-bench/windows-lan-dhcp-inventory-20260526T030425Z.json`
  - `research/bench-records/live-bench/windows-lan-dhcp-apply-20260526T030457Z.json`
  - `research/bench-records/live-bench/windows-lan-dhcp-ssh-scan-20260526T030513Z.json`
  - `research/bench-records/live-bench/espnow-bbs-lan-dhcp-current-remap-preflight-20260526T030752Z.json`
  - `research/bench-records/live-bench/espnow-bbs-lan-dhcp-current-remap-preflight-20260526T032217Z.json`

## Verified Facts

- Pre-change Windows state had HNetCfg Internet Connection Sharing enabled with
  `Wi-Fi` as public sharing type `0` and `Ethernet 3` as private sharing type
  `1`.
- Pre-change `Ethernet 3` was static `192.168.137.1/24`.
- Windows CP210x inventory contained exactly `COM6`, `COM10`, and `COM12`.
- HNetCfg `DisableSharing()` succeeded for both `Wi-Fi` and `Ethernet 3`.
- `Ethernet 3` was set to IPv4 DHCP, DNS was reset, and DHCP renew returned
  `192.168.200.225/24` with gateway `192.168.200.1`.
- LAN SSH discovery found one TCP 22 candidate at `192.168.200.153`.
- `192.168.200.153` presented the expected `dos-pi4-poe` RSA, ECDSA, and
  ED25519 SSH fingerprints.
- The updated preflight used the current accepted peer remap:
  `peer01=COM6`, `peer02=COM10`, `peer03=COM12`.
- Read-only identity matched the accepted peer MACs:
  - `peer01` `94:b9:7e:da:17:d0` on `COM6`
  - `peer02` `78:e3:6d:0a:90:14` on `COM10`
  - `peer03` `94:b9:7e:da:9a:50` on `COM12`
- Pi `/dev/ttyUSB0` read-only identity matched coordinator MAC
  `78:e3:6d:10:4d:6c` with 4 MB flash.
- Fresh pre-cleanup SSH keyscan/known-hosts verification on 2026-05-26 UTC
  matched the expected Pi RSA, ECDSA, and ED25519 fingerprints.
- The only pre-cleanup related Pi process was PID `1346` with command
  `dosbox-x -conf /home/dospi/dos-c/devices/pi4-poe/win31.conf -nomenu -fullscreen`.
- No Pi listener was present on `31331`, `31332`, or `8080` before cleanup.
- A normal `SIGTERM` removed PID `1346`; no `SIGKILL` was used.
- Post-cleanup checks found no related DOSBox-X, DOSBox, bridge, modal, or
  `31331`/`31332`/`8080` listener state.
- Post-cleanup read-only preflight
  `research/bench-records/live-bench/espnow-bbs-lan-dhcp-current-remap-preflight-20260526T032217Z.json`
  returned `ok:true` with no failures and summary readiness
  `ready_for_prepare`.
- No prepare, flash, erase, monitor, bridge, DOSBox-X launch, Win31/OPCON run,
  PCAP, router/admin mutation, BLE, mesh, relay, XBee, TFT, MicroSD, load, or
  mains action was run after the preflight stop.

## Assumptions

- The accepted transport path remains
  `OPCON.EXE -> COM1 -> DOSBox-X nullmodem -> Pi bridge -> /dev/ttyUSB0 -> ESP32 coordinator`.
- Since `/dev/ttyUSB0` is already the expected coordinator MAC, the recovery
  flash path should remain closed unless new identity evidence changes.
- Cleanup authorization was limited to graceful termination of the exact stale
  process only.

## Unknowns

- Physical USB-only/no-load/no-relay/TFT/MicroSD/XBee wiring state is not proven
  by the read-only preflight.
- No live ESP-NOW radio traffic is generated by this preflight.

## Stop Gate

- Initial preflight stopped with `ok:false` because Pi process `1346 dosbox-x
  -conf /home/dospi/dos-c/devices/pi4-poe/win31.conf -nomenu -fullscreen` was
  still running.
- The stale process blocker is resolved by graceful cleanup and a fresh
  `ok:true` read-only preflight.
- Do not run `prepare` or `flash`: coordinator identity is already correct.
- Do not start a bridge/DOSBox-X/Win31 proof run unless a new explicit proof
  run is requested.

## Validation

- `python3 -m unittest tests.live_bench.test_multipeer_preflight tests.live_bench.test_espnow_bbs_live_gate`: passed after tooling updates.
- Final read-only preflight:
  `research/bench-records/live-bench/espnow-bbs-lan-dhcp-current-remap-preflight-20260526T030752Z.json`
  returned `ok:false` only because of the stale Pi `dosbox-x` process.
- Cleanup resume checks:
  - Fresh SSH keyscan/known-hosts verification matched the expected Pi
    fingerprints.
  - Pre-cleanup process/listener check found only the exact PID `1346`
    `dosbox-x` blocker and no `31331`/`31332`/`8080` listeners.
  - `SIGTERM` removed PID `1346`; post-cleanup process/listener check was clear.
- Post-cleanup read-only preflight:
  `research/bench-records/live-bench/espnow-bbs-lan-dhcp-current-remap-preflight-20260526T032217Z.json`
  returned `ok:true` with no failures.
